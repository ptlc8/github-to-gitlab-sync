{
	"name": "github-to-gitlab-sync",
	"version": "1.0.0",
	"description": "Sync all repos from GitHub to GitLab",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"start": "node dist/index.js",
		"dev": "tsc --watch",
		"check:types": "tsc --noEmit",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [
		"sync",
		"github",
		"gitlab",
		"git"
	],
	"author": "Ambi",
	"license": "ISC",
	"dependencies": {
		"@gitbeaker/rest": "^43.8.0"
	},
	"devDependencies": {
		"@types/node": "^25.2.3",
		"typescript": "^5.9.3"
	}
}
